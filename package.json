{
  "name": "x14-plus-project-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent local and SSH project tools for ChatGPT MCP",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "stdio": "node src/server.mjs --stdio",
    "setup": "node scripts/setup.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.5"
  }
}
